---
import { getEntry, render } from 'astro:content';
import PageLayout from '../layouts/PageLayout.astro';

const entry = await getEntry('pages', 'affiliate-disclosure');
if (!entry) throw new Error('Missing content/pages/affiliate-disclosure.md');
const { Content } = await render(entry);
---

<PageLayout title={entry.data.title} description={entry.data.description} path="/affiliate-disclosure/" updatedDate={entry.data.updatedDate}>
  <Content />
</PageLayout>
