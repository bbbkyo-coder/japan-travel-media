---
import { getEntry, render } from 'astro:content';
import PageLayout from '../layouts/PageLayout.astro';

const entry = await getEntry('pages', 'privacy-policy');
if (!entry) throw new Error('Missing content/pages/privacy-policy.md');
const { Content } = await render(entry);
---

<PageLayout title={entry.data.title} description={entry.data.description} path="/privacy-policy/" updatedDate={entry.data.updatedDate}>
  <Content />
</PageLayout>
